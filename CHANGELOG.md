## [1.0.0-alpha.0] - 2025-09-14

### 🚀 Features

- *(app)* Implement MCP agent with hello world tool

### 🐛 Bug Fixes

- Solve some minor issues and update dependencies

### 📚 Documentation

- *(agents)* Add repository contribution guidelines
- *(funding)* Add funding configuration
- *(templates)* Add issue templates for bugs and features
- *(readme)* Add badges and project links
- *(templates)* Update issue templates for clarity
- *(ci)* Improve stale bot configuration comments
- *(readme)* Enhance project description and features

### ⚙️ Miscellaneous Tasks

- Add LICENSE
- Add package.json
- Add `.gitignore` file
- Add `pnpm-workspace.yaml`
- Add `pnpm-lock.yaml`
- *(config)* Add `biome.json` configuration
- *(config)* Add `tsconfig.json` configuration
- *(config)* Add `wrangler.jsonc` for Cloudflare Workers setup
- *(config)* Update `tsconfig.json` types configuration
- *(scripts)* Add npm scripts for dev, lint, and type-check
- *(types)* Add `cloudflare-env.d.ts` for type definitions
- *(config)* Enable `resolveJsonModule` in `tsconfig.json`
- *(config)* Add `.editorconfig` for consistent formatting
- *(style)* Adjust indentation to ensure consistency
- *(style)* Update `biome.json` indentation to spaces
- *(style)* Update indentation across project files
- Update minimum Node.js version to 20.6.0
- *(config)* Add `cliff.toml` for changelog generation
- *(config)* Update `.gitignore` to include `.wrangler`
- *(config)* Update `compatibility_date` in `wrangler.jsonc`
- *(scripts)* Add `test` script for running `vitest`
- *(config)* Add `vitest` configuration for workers
- *(scripts)* Add `build` script for testing deployments
- *(ci)* Add Dependabot configuration for dependencies
- *(ci)* Add stale bot configuration
- *(workflows)* Add Git Flow and Build Pipeline workflows
- *(workflows)* Add template cleanup workflow
- *(workflows)* Refine template cleanup workflow
