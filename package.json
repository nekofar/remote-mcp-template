{
  "name": "remote-mcp-template",
  "version": "1.0.0-alpha.15",
  "description": "Template for a Model Context Protocol (MCP) server.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "format": "biome format --write",
    "lint": "biome lint",
    "lint:fix": "biome lint --fix",
    "start": "wrangler dev",
    "test": "vitest --run",
    "cf-typegen": "wrangler types --include-runtime=false --strict-vars=false --env-interface CloudflareEnv ./cloudflare-env.d.ts",
    "type-check": "tsc --noEmit",
    "inspector": "mcp-inspector --transport sse --server-url http://127.0.0.1:8787/sse"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "server",
    "template"
  ],
  "author": "Milad Nekofar <milad@nekofar.com>",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=20.6.0"
  },
  "packageManager": "pnpm@10.20.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.24.3",
    "agents": "0.2.32",
    "hono": "4.11.0",
    "zod": "4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@cloudflare/vitest-pool-workers": "0.10.3",
    "@cloudflare/workers-types": "4.20251210.0",
    "@modelcontextprotocol/inspector": "0.17.5",
    "@types/node": "24.10.2",
    "ts-node": "10.9.2",
    "typescript": "5.9.3",
    "vitest": "4.0.15",
    "wrangler": "4.16.1"
  }
}
